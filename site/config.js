window.WINUTIL_CONFIG = {
  // Change this only if you expose the local agent on another port.
  agentUrl: "http://127.0.0.1:15721",
  // Static site origin must also be present in agent appsettings.json.
  siteName: "WinUtil NG",
  version: "0.2.1"
};
